plugins {
    id 'java'
    id 'org.jetbrains.intellij' version '0.7.2'
}

group 'co.simpleq'
version '0.0.8'

repositories {
    mavenCentral()
}

publishPlugin {
    token intellijPublishToken
}

dependencies {
    compile group: 'org.apache.maven', name: 'maven-artifact', version: '3.6.2'
    testCompile group: 'junit', name: 'junit', version: '4.12'
}

intellij {
    version '2021.1'
    plugins 'maven'
}
patchPluginXml {
    changeNotes """
      0.0.8 Release.<br>
      <em>intellij idea version 2021.1 support added.</em>"""
}